[build]
builder = "nixpacks"
buildCommand = "npm install && npx medusa db:migrate"

[deploy]
startCommand = "npx medusa start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
